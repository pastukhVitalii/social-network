let initialState = {
  bestFriends: [
    {id: 1, name: 'Dimych'},
    {id: 2, name: 'Roman'},
    {id: 3, name: 'Mark'}
  ]
}

const navBarReducer = (state = initialState, action) => {
  return state;
}

export default navBarReducer;